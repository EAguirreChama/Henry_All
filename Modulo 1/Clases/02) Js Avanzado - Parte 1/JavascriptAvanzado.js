var En = "Hi"

function Person () {
    var Name = "FT"
    var Last = "37a"

    function firstName (){ 
        return Name 
    }

    function lastName () {
        return Last
    }

    console.log(En + " " + firstName() + " " + lastName())
}

Person()
