// Require se utiliza para traer las constantes de otro archivo
// const {num1, num2, nombre} = require("./constantes")
// Se utiliza destructuring
// console.log(constantes);

export const sumar = (a, b) => {
    console.log(a + b)
}

export const printName = (name) => {
    console.log(name)
}
